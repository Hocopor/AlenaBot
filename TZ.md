# ТЗ: Психологический телеграм-бот (Алёна — психолог-СоПутница)

## Общее описание
Телеграм-бот для психолога Алёны, призванный мягко вовлекать пользователей в диалог о внутреннем состоянии и предлагать персональные инструменты поддержки (гайды, музыку, дыхательные упражнения, маркеры тревоги, челленджи и информацию о живой группе «Перерождение»).

Бот должен быть развернут на платформе Express (Node.js + TypeScript). 
Будет запускаться на Ubuntu 24. В `.env` задается домен (например, `APP_URL`).
Бот должен поддерживать работу через вебхуки (привязанные к этому домену), а если вебхуки не работают — переключаться на режим polling (длинный опрос), чтобы бот не падал.

---

## Подробный сценарий и логика сообщений

### 1. Первый переход в бота (До старта)
Когда клиент переходит в бота в первый раз, но ещё не нажал "Старт", бот должен выдавать вступительное сообщение-призыв:
> "Добро пожаловать ☺️
> Для продолжения нажмите кнопку "Старт" или /start"
*(Примечание: это сообщение может показываться на любые отличные от `/start` команды до инициализации, либо при попытке написать текстовое сообщение).*

### 2. Приветственный цикл (После старта)
После нажатия кнопки **"/start"** (или отправки команды `/start`):
1. Бот отправляет **первое сообщение** (Приветствие):
   > "Привет. Рада, что ты здесь🤍
   > 
   > Я Алёна — психолог-СоПутница. Помогаю мягко распутать то, что внутри давно накопилось. Без давления и осуждения.
   > 
   > Прежде чем отдам тебе шаги - инструменты, я хочу понять, что сейчас происходит внутри. Два ключевых вопроса, которые помогут подобрать решение именно под тебя.
   > 
   > Это место — безопасное. Можно выдохнуть."

2. **Через 15 секунд** отправляется **второе сообщение** с инлайн-кнопками:
   > "Скажи честно — что из этого сейчас про тебя?"
   
   **Инлайн-кнопки (кнопки под сообщением):**
   * `😮‍💨 Устала, но продолжаю тянуть`
   * `😶 Всё серое — и не знаю почему`
   * `😰 Тревога, которая не отпускает`
   * `💭Не понимаю себя и что со мной происходит`
   * `🌀 Всё сразу`

   **Логика работы кнопок:**
   * Нажатие на кнопку сохраняет выбор (у пользователя в переписке визуально сохраняется его выбор, например, кнопка выделяется галочкой или убираются другие кнопки, чтобы осталось выбранное состояние).
   * Выбранное значение никуда в БД сохранять не нужно. Это чисто для пользовательской визуализации.
   * После нажатия на любую кнопку отправляется **третье сообщение**, но строго **один раз** за цикл (повторные клики по кнопкам из старого сообщения не должны отправлять новые сообщения или дублировать логику, пока не будет нажат `/start` заново).

3. **Третье сообщение**:
   > "А внутри чаще всего звучит что-то из этого?"
   
   **Инлайн-кнопки:**
   * `Я просто ленивая`
   * `Надо взять себя в руки`
   * `У других всё нормально — только у меня так`
   * `Я не знаю, чего хочу`

   **Логика:**
   * После нажатия кнопка визуально выделяется (показывает выбор), повторные нажатия ничего не отправляют.
   * Сразу после этого отправляется **четвертое сообщение**.

4. **Четвертое сообщение** (Главное меню инструментов):
   > "Слышу тебя 🩶
   > Всё это — не слабость и не лень. Это сигнал. Тело и душа просят опоры.
   > Выбери, с чего хочешь начать ⬇️"
   
   **Инлайн-кнопки (Главное меню):**
   1. `ДНЕВНИК МИКРО-ПОБЕД — Гайд`
   2. `АУДИО ВРЕМЯ — музыка`
   3. `УПРАЖНЕНИЕ   —  техники`
   4. `ОПОРА`
   5. `ХОЧУ — Челленджи`
   6. `🩶 ГРУППА «ПЕРЕРОЖДЕНИЕ»`

---

### 3. Скрывающееся (Reply) меню
* **До прохождения приветственного диалога (до 4-го сообщения)**: в скрывающемся меню (Reply Keyboard) внизу экрана находится только одна кнопка:
  * `Старт` (или `/start`)
* **С момента отправки 4-го сообщения**: в скрывающемся меню появляется вторая кнопка:
  * `Вернуться в меню`
  Теперь в нем две кнопки: `Старт` и `Вернуться в меню`.
* Нажатие на кнопку `Вернуться в меню` отправляет сообщение:
  > "Сделай свой выбор ⬇️"
  И выводит те же самые 6 инлайн-кнопок Главного меню.

---

### 4. Ветки сценария (6 вариантов)

#### Ветка 1. ДНЕВНИК МИКРО-ПОБЕД — Гайд
1. При нажатии отправляется:
   > "ДНЕВНИК МИКРО-ПОБЕД — Гайд"
   
   *Инлайн-кнопка:* `1 - Гайд легализации бездействия🩶`
2. При нажатии на эту кнопку отправляется сообщение:
   > "Сначала — одна мысль.
   > Бездействие это не провал. Иногда это кажется большим, на то, что хватает силы. И это честно.
   > Внутри дневника — маленькие шаги. Таких, чтобы не надо было «брать себя в руки».
   > Просто — чуть бережнее к себе. День за днём."
3. Следом сразу отправляется сообщение со **ссылкой на скачивание дневника** *(пока заглушка)*.
4. **Через 10 секунд** отправляется сообщение:
   > "Надеюсь, он станет твоим маленьким другом 
   > Если почувствуешь, что хочется глубже — я рядом. В июне открываю живую группу «Перерождение». Напиши мне — поговорим подробнее."
   
   *Инлайн-кнопки:*
   * `Написать` (ссылка-заглушка на ЛС в телеграме)
   * `«Вернуться в меню»` (инлайн-кнопка возврата)

#### Ветка 2. АУДИО ВРЕМЯ — музыка
1. Отправляется сообщение:
   > "**Аудио библиотека**
   > Включай — и просто побудь. Ничего делать не нужно.
   > Это твои несколько минут только для тебя."
2. Сразу следом отправляется **аудифайл** или голосовое сообщение *(заглушка)*.
3. **Через 10 секунд** отправляется:
   > "Побудь в этом состоянии чуть дольше 
   > А если захочешь — загляни в другие материалы. Там есть реальность, дневник и кое-что ещё ⬇️"
   
   *Инлайн-кнопка:* `«Вернуться в меню»`

#### Ветка 3. УПРАЖНЕНИЕ — техники
1. Отправляется сообщение-заголовок (жирным):
   > "**УПРАЖНЕНИЕ   —  техники**"
   
   *Инлайн-кнопка:* `«Квадрат Дыхания»  аудио`
2. При нажатии отправляется:
   > "Упражнение за 2 минуты успокаивает нервную систему.
   > Включай прямо сейчас. Можно лёжа."
   
   *(А также отправляется файл/аудиофайл или ссылка-заглушка).*
3. **Через 7 секунд** отправляется сообщение:
   > "Как ты? 
   > Сохрани аудио — и возвращайся каждый раз, когда найдешь. Это работает.
   > Если хочешь понять глубже — почему тревога возвращается снова и снова — напиши мне. Поговорим."
   
   *Инлайн-кнопки:*
   * `Написать` (ссылка-заглушка на ЛС)
   * `«Вернуться в меню»`

#### Ветка 4. ОПОРА
1. Отправляется сообщение-заголовок (жирным):
   > "**ОПОРА**"
   
   *Инлайн-кнопки:*
   * `1 - Маркер  Тревоги`
   * `2 - Фразы Поддержка`
2. При нажатии на `1 - Маркер  Тревоги`:
   * Отправляется сообщение:
     > "**Маркер Тревоги** 
     > Этот простой инструмент — поможет понять, что сейчас происходит внутри. Тревога ,страх или апатия.
     > Когда узнаешь — становится чуть легче. Уже не «со мной что-то не так», а просто — вот что сейчас есть."
     *(А также отправляется файл или ссылка-заглушка на материал).*
   * **Через 10 секунд** отправляется:
     > "Теперь ты знаешь чуть больше о себе. 
     > Это уже немаловажно. Если хочешь — следующий шаг: реальный Квадрат. Оно помогает прямо в данный момент."
     *Инлайн-кнопка:* `«Вернуться в меню»`
3. При нажатии на `2 - Фразы Поддержка`:
   * Отправляется сообщение:
     > "**Фразы Поддержка**
     > Стань переводчиком для своего ребенка.
     > 12 фраз которые открывают диалог без давления и осуждения."
     *(А также файл или ссылка-заглушка).*
   * **Через 10 секунд** отправляется:
     > "Возвращайся к ним в любой момент 
     > И помни — слова работают, когда мы готовы их услышать. Сегодня ты была готова.
     > Если захочется большего — я здесь. Живая группа «Перерождение» всегда открыта."
     *Инлайн-кнопка:* `«Вернуться в меню»`

#### Ветка 5. ХОЧУ — Челленджи
1. Отправляется сообщение-заголовок (жирным):
   > "**ХОЧУ — Челленджи**"
   
   *Инлайн-кнопки:*
   * `7 дней к себе`
   * `14 дней к себе`
2. При нажатии на `7 дней к себе`:
   * Отправляется сообщение:
     > "**7 дней к себе** 
     > Отлично, что ты здесь 
     > Челлендж — это не «заставить себя». Это маленькое приключение к себе.
     > Каждый день — одно простое действие. Без давления. Без оценок. Просто попробуй — каково это, когда к себе по-доброму."
   * **Через 5 секунд** отправляется сообщение:
     > "Доступ к челленджу: [Ссылка]" *(заглушка)*
   * **Через 10 секунд** отправляется:
     > "Ты решилась — и это уже шаг 🤍
     > Веди дневник рядом — так будет виднее, как ты меня. Даже когда кажется, что ничего не происходит."
     *Инлайн-кнопка:* `«Вернуться в меню»`
3. При нажатии на `14 дней к себе`:
   * Отправляется сообщение:
     > "**14 дней к себе** 
     > Отлично, что ты здесь 
     > Челлендж — это не «заставить себя». Это маленькое приключение к себе.
     > Каждый день — одно простое действие. Без давления. Без оценок. Просто попробуй — каково это, когда к себе по-доброму."
   * **Через 5 секунд** отправляется:
     > "Доступ к челленджу: [Ссылка]" *(заглушка)*
   * **Через 10 секунд** отправляется:
     > "Ты решилась — и это уже шаг 🤍
     > Веди дневник рядом — так будет виднее, как ты меня. Даже когда кажется, что ничего не происходит."
     *Инлайн-кнопка:* `«Вернуться в меню»`

#### Ветка 6. 🩶 ГРУППА «ПЕРЕРОЖДЕНИЕ»
1. Отправляется сообщение:
   > "Рада, что ты здесь 
   > «Перерождение» — это живая группа. Всего 10 мест. 
   > Здесь не будет лекций и домашних занятий. Только живая работа — мягко, в своем темпе, с обратной связью от меня.
   > Для тех, кто давно думал: что-то должно измениться. Но непонятно — с чего начать и хватит ли сил.
   > Хочешь узнать подробнее — напиши мне лично. Расскажу всё."
   
   *Инлайн-кнопка:* `Написать` (ссылка-заглушка)
2. **Через 10 секунд** отправляется:
   > "Буду ждать твоих сообщений 🤍
   > Не торопись — просто знай, что место есть. И оно может быть твоим."
   
   *Инлайн-кнопки:*
   * `Написать` (ссылка-заглушка)
   * `«Вернуться в меню»`

---

## Технические требования
1. **Язык**: TypeScript, Node.js + Express.
2. **Окружение**: Ubuntu 24.
3. **Вебхуки и Polling**:
   * Бот должен пытаться установить вебхук на `APP_URL / webhook` (домен задается через переменные среды).
   * Если домен не задан или не удается настроить вебхуки (например, нет SSL или сеть заблокирована), бот автоматически переходит на режим `long polling`, не крашась при этом и продолжая исправно работать.
4. **Безопасность**:
   * Токен телеграм-бота и домен должны передаваться через `.env` переменные: `TELEGRAM_BOT_TOKEN` и `APP_URL`.
   * Токен должен валидироваться перед запуском.
5. **Файлы**:
   * Сценарий и тексты должны быть вынесены в отдельный конфигурационный/сервисный файл для простоты будущего редактирования ссылок и путей к реальным файлам психологических материалов.
